//Declarando as constantes
const base = 5;
const heigth = 8;

//Declarando as constantes e já atribuindo o cálculo de área e perimetro
const area = base * heigth;
const perimeter = 2 * (base + heigth);

//Imprimindo o resultado
console.log(base);
console.log(heigth);
console.log(area);
console.log(perimeter);
